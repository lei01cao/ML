Exercise Conclusion:

Logistic回归，此处用的是1/m的形式，除以了样本数目
(目的主要是  1.为了防止样本数量过大导致运算溢出！！2.归一化代价函数，不会因为m的增长而增长)

最大似然率直接推出的求最大logL(θ) → 统一的求最小-logL(θ)
(便于与梯度下降统一算法)

mapFeature函数：将原始的2个特征向量(两个问答测试的得分结果)转化成28维(pasical三角, sum(1~(6+1)) )
的特征向量(按幂次数1~6排列组合添加，外带初始化的0维部分)
也被称为“离散化”

正则化修改后的特征向量时，注意此处λ后跟的θ不包含θ0(常数)部分！！！
(或者将theta向量的第一个元素替换为0。题目要求，不唯一)

