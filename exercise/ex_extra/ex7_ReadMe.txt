Exercise Conclusion:


12-21 k-Means算法伪码的直译版。
注意这里全部默认聚类数目k为已知参数，收敛性判断只依据max_iters（i.e. 不会自己判断两次迭代改变是否小到满足收敛条件中途退出）。

随机初始化聚类中心：直接训练样本随机排序取前k个


PS: 图像重聚类部分（157~161行）添加可自由筛选前x类绘制颜色，后续聚类着色统一为白色(255,255,255)


PCA

实现求特征向量这里用SVD。 U即所需的特征向量组成矩阵(列向量)，S奇异值对应对角阵(大→小)（注意是对标准化后(Z-score)的训练样本的协方差阵）

PS：一般情况下，矩阵X做SVD分解[U, S, V] = svd(X)，X'*X的特征向量矩阵是V(列向量)。此处的Σ恒为对称方阵(特征值差负号) 求得的U与V其实等效为特征向量
PPS：此处仅是根据ex7要求进行SVD求解（即先求取协方差阵后对协方差阵进行SVD，题目未说明清楚，此时SVD等效于对Σ做特征分解）。实际我们关心X'*X的特征向量，对X进行SVD，取出V前k列！！！！！


